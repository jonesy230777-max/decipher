import { EmptyPage } from "./_empty";
export default function CohortInsights() {
  return <EmptyPage title="Cohort Insights" body="Distribution charts and validated patterns arrive in M9 once the cohort analyser and pattern hunter populate pattern_library." />;
}
