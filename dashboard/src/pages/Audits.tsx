import { EmptyPage } from "./_empty";
export default function Audits() {
  return <EmptyPage title="Audits" body="Table arrives in M3 once the audit lifecycle endpoints are wired. Filter, sort, detail drawer." />;
}
