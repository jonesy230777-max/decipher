import { EmptyPage } from "./_empty";
export default function SquarespaceExport() {
  return (
    <EmptyPage
      title="Squarespace Export"
      body="Generate New Export button + preview tree + Download Bundle (.zip) arrive in M10. This is the single most important page on the dashboard: the contract between the local prototype and the public site."
    />
  );
}
