import { EmptyPage } from "./_empty";
export default function Teams() {
  return <EmptyPage title="Teams" body="Executive teams arrive in M6. View-as capability so Steve can see what each sales director sees." />;
}
